INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
SET(PBFT_TEST_FILES Main.cpp)
ADD_EXECUTABLE(pbfttest ${PBFT_TEST_FILES})
SOURCE_GROUP("Source Files" FILES ${PBFT_TEST_FILES})
TARGET_LINK_LIBRARIES(pbfttest pbft)
